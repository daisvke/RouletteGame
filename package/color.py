"""A module that works with the colors from the roulette numbers."""

from package.board import *

def define_nb_clr():
	"""A Function that fixes the color according to the number."""
	nb_clr = ["vert", 
		"rouge", "noir", "rouge", 
		"noir", "rouge", "noir",
		"rouge", "noir", "rouge",
		"noir", "noir", "rouge",
		"noir", "rouge", "noir",
		"rouge", "noir", "rouge",
		"rouge", "noir", "rouge",
		"noir", "rouge", "noir",
		"rouge", "noir", "rouge",
		"noir", "noir", "rouge",
		"noir", "rouge", "noir",
		"rouge", "noir", "rouge"]

	return nb_clr

